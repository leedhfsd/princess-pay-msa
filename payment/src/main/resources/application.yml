spring:
  application:
    name: payment

  config:
    import: "optional:configserver:http://localhost:8888"

  cloud:
    config:
      name: payment
      profile: local

#eureka:
#  client:
#    serviceUrl:
#      defaultZone=http://localhost:8761/eureka: